stages:     
  - build
  - test

build-job:       
  stage: build
  tags:
    - vm-shell
  script:
    - echo "Compiling the code..."
    - pwd
    - touch myfile.txt
    - ls
  artifacts:
    paths:
      - ./myfile.txt

unit-test-job: 
  tags:
    - vm-shell
  stage: test    
  script:
    - echo "Running unit tests..."
    - ls
    - test -f myfile.txt
